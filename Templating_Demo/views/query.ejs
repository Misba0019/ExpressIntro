<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= name %></title>
    <link rel="stylesheet" href="/app.css">
</head>
<body>
    
    <h1>Browsing the <%= name %> page</h1>
    <!-- Here we browsed our subQuery/name from data.json file -->

    <h2><%= description %></h2>
    <!-- description of the data.json file -->

    <p><%= subscribers %> : Total Subs</p>
    
    <hr>

    <% for(let post of posts) { %>
        <article>
            <p><%= post.title %> - <b><%= post.author %></b></p>
            <% if(post.img){ %>
                <img src="<%= post.img %>" alt="">
                <!-- Render the image only if the post has an 'img' property. This ensures that the img tag is included ONLY when there is an image Available for the post -->
                <% } %>
        </article>
        <% } %>
</body>
</html>